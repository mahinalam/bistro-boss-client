import React from 'react';

const TableRow = ({ item,index }) => {
    const { email,date,status } = item;

    return (
        <div className='w-full'>
        
        </div>
    );
};

export default TableRow;